{
  "name": "50x50-contract",
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.ts --network blast-sepolia",
    "verify": "npx hardhat verify --network blast-sepolia <c> 50"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.4.2",
    "node-dotenv": "^1.1.1"
  }
}
